<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo2_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollabUp"
        tools:targetApi="31">

        <activity
            android:name=".controlador.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".controlador.LoginController"
            android:exported="false" />

        <activity
            android:name=".controlador.SingupController"
            android:exported="false" />

        <activity
            android:name=".controlador.AppController"
            android:exported="false" />
        <activity
            android:name=".controlador.EditProfileActivity"
            android:exported="false" />
        <activity
            android:name=".controlador.AddCollabViewActivity"
            android:exported="false" />


    </application>

</manifest>